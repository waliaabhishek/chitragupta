import type { ColDef } from "ag-grid-community";

export const GRID_THEME_CLASS = "ag-theme-alpine ag-theme-chitragupta";

export const defaultColDef: ColDef = {
  sortable: true,
  resizable: true,
};
